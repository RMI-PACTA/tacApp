#' Private data added to .gitignore
"valid"

#' Private data added to .gitignore
"useful"

#' All `rowid` of the `valid` dataset that produces valid results
#'
#' Results are valid if they have at least one real or virtual category.
"valid_rowids"

#' Unique `rowid` of the `valid` dataset that produces valid results
#'
#' Results are valid if they have at least one real or virtual category.
"unique_rowids"
